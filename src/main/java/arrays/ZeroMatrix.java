package arrays;

public class ZeroMatrix {

  /**
   * If an element in Matrix M x N is zero then its entire row and column becomes zero.
   * @param input
   * @return
   */
  public int[][] makeZero(int[][] input){
    int[][] output = null;
    return output;
  }

}
